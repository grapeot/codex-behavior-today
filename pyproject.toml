[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-behavior-today"
version = "0.1.0"
description = "A daily public monitor for observed Codex endpoint behavior"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
